// Modules
const names = require('./3-names')
const sayHi = require('./4-utils')
//console.log(names)

const data = require('./5-alternative-flavors') //{ items: [ 'item1', 'item2' ], singlePerson: { name: 'bob' } }
// console.log(data)

require('./6-mine-grenade')

// sayHi('susan')
// sayHi(names.john)
// sayHi(names.peter)